#include <stdio.h>
int main()
{

    FILE *ptr = NULL;
    // char string[69];
    // ptr = fopen("file.txt", "r");
    // for reading the file use this
    // fscanf(ptr, "%s", string);
    // printf("%s", string);
//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    // for writing  in this file we use this
    // char string[69] = "this is a file operation so be prepared ";
    // ptr = fopen("file.txt", "w");
    // fprintf(ptr, "%s", string);
//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

 // for writing without deleting the last sentence we use this
//     char string[69] = "this is a file operation so be prepared ";
//     ptr = fopen("file.txt", "a");
//     fprintf(ptr, "%s", string);

    return 0;
}